apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fl-demonstrator.fullname" . }}-test-connection"
  labels:
    {{- include "fl-demonstrator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fl-demonstrator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
