apiVersion: v1
kind: Service
metadata:
  name: {{ include "fl-demonstrator.fullname" . }}
  labels:
    {{- include "fl-demonstrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fl-demonstrator.selectorLabels" . | nindent 4 }}
